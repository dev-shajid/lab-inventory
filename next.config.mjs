/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        domains: ['images.unsplash.com','preschool.dreamguystech.com', 'res.cloudinary.com'],
    }
};

export default nextConfig;
